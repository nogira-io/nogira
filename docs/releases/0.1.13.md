# NoGira 0.1.13

**Release date:** 2026-06-02

Documentation update to support the **NoGira MCP Integration Platform** (**NGF-026**): how to connect AI clients (Cursor) to your NoGira MCP endpoint, what tools exist (MVP1), and basic security guidance.

## Added

- **MCP public guide** — `docs/mcp.md`: what MCP is, URLs, auth model (PAT passthrough), MVP1 tool list, and Cursor setup (Streamable HTTP). Includes screenshot placeholders (**coming soon**).

## Changed

- **Deployment docs** — `docs/deployment.md`: adds MCP endpoint URLs and links to the MCP guide.
- **Quick start** — `docs/quick-start.md`: links to the MCP guide under Next steps.

## Upgrade notes

- This installer update is **documentation-only**. It does not change runtime behavior by itself.
- If you are upgrading application images, keep **core**, **UI**, and **notification-worker** on the **same semver** as documented in the versioned release notes.

**Tracking:** **NGF-026** (MCP platform) docs tranche — **NG-706**.

